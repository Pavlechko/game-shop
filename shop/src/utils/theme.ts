import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#007aff',
      contrastText: '#fff',
    },
    secondary: {
      main: '#007aff',
      contrastText: '#FFF',
    },
  },
});
